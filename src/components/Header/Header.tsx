import './scss/Header.scss'

const Header = () => {
    return (
        <div>
            <div id="mainHeader">Pattern Finder</div>
            {/* <p>Base URL: <code><a href={ import.meta.env.VITE_API_URL } target="_blank">{ import.meta.env.VITE_API_URL }</a></code></p> */}
        </div>
    )
}

export default Header;
